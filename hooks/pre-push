#!/usr/bin/env bash
# Thin wrapper: all composition logic lives in _dispatch.
# Add gates by dropping numbered scripts into hooks/pre-push.d/ — do not edit this file.
exec "$(dirname "$0")/_dispatch" pre-push "$@"
