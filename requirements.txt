# Dependencies for the repository's Python validation scripts.
#
# jsonschema's Draft 2020-12 behaviour (oneOf / $defs resolution, which the
# contract schemas rely on heavily) is version-sensitive, so the lower bound
# is pinned to a release with stable 2020-12 support.
jsonschema>=4.18,<5
PyYAML>=6.0,<7

# CommonMark parsing for local-link and embedded-example checks. Version 4 is
# production/stable, supports Python 3.10+, and resolves reference links.
markdown-it-py>=4.0,<5
